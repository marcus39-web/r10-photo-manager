
<Page x:Class="R10CSharp.Pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:R10CSharp.Pages"
      Background="White">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="28" Width="720" MaxWidth="900">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Willkommen" FontSize="32" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryTextBrush}" />

                <Image Source="/R10CSharp;component/Assets/welcome.JPEG"
                       Width="520"
                       Height="300"
                       Stretch="Uniform"
                       Margin="0,0,0,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
